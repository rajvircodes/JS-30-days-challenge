# Day 05 — Chunk Array

### Problem
Given an array and a number `size`, divide the array into many subarrays where each subarray has a maximum length of `size`.

### Example
Input: [1,2,3,4,5], size: 2  
Output: [[1,2], [3,4], [5]]

### Task
Write the function `chunkArray(arr, size)` inside `challenge.js`.

### Run
node challenge.js
